// Questão 5

/* 5 - Escreva um algoritmo que permita ao usuário
calcular quantos centímetros uma criança cresce por ano
a partir das diferenças de altura e de idade das medições realizadas.
*/

import java.util.ArrayList;
import java.util.Scanner;

public class Qt5 {
    public static void main(String[] args) {
        /* teste
        Scanner leitor = new Scanner(System.in);
        ArrayList<Double> alturas = new ArrayList<>();
        System.out.println("Informe a altura incial em cm: ");
        alturas.add(leitor.nextDouble());
        System.out.println("Informe a altura seguinte  em cm: ");
        alturas.add(leitor.nextDouble());
        while (true){
            System.out.println("Deseja informar mais uma altura? (Digite S para ou N não)");
            String resposta = leitor.next();
            if(resposta.equalsIgnoreCase("S")){
                System.out.println("Informe a nova altura em cm: ");
                alturas.add(leitor.nextDouble());
            } else {
                break;
            }
        }
        System.out.println("Crescimento: ");
        System.out.printf("1° ano (início): %.2fcm\n", alturas.get(0));
        for(int i = 1; i < alturas.size(); i++){
            System.out.printf("%d° Ano: %+.2fcm\n", i+1, alturas.get(i) - alturas.get(i-1));
        }
        */
    }
}